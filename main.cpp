#include <iostream>
#include "Stack.hpp"

/*
int main() {
    Stack s;

    std::cout << "Empty or not : " << s.empty() << endl;

    s.push((int) 1);
    s.push((int) 2);
    s.push(3);

    s.print();

    std::cout << "Top is " << s.top() << "Empty or not: " << s.empty() << "Full or not: " << s.full() << endl;

    s.pop();

    s.print();

    std::cout << "Hello, World!" << std::endl;

    for (int i = 2; i < 10; i++) {
        s.push(5);
    }

    s.print();
    std::cout << "Full or not : " << s.full() << endl;


    return 0;
}
 */